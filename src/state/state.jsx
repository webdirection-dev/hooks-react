import React, {useState} from "react";

const State = () => {
    const [value, setValue] = useState(0);

    return(
        <div>
            <h1>Test</h1>
        </div>
    )
};

export default State;